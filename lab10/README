Hiya,

Topic: Poll client server application to multiplex TCP and UDP requests.
Compile: gcc file -o binary
Instructions to run:
  1. First, run the server binary (./server) to start the server.
  2. Then, run the TCP client binary (./tcp_client).
  3. And finally, run the UDP client binary (./udp_client).
